<div class="flex flex-col items-center">
    <AlbumCard Album="Album"></AlbumCard>
    <div class="mt-2 flex justify-center w-full">
        <div class="ml-2 flex text-lg">
            @for (int i = 1; i <= 10; i++)
            {
                <span class="@(i <= Album.UserRating ? "text-yellow-400" : "text-gray-400") cursor-pointer"
                    @onclick="() => SetRatingAsync(i)">
                    ★
                </span>
            }
        </div>
    </div>
</div>